# Development dependencies — NOT needed in production
# Install with: pip install -r requirements-dev.txt

-r requirements.txt

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Linting & formatting
ruff>=0.4.0

# Standalone tools (Windows-only, used outside Azure Functions)
rapidfuzz>=3.0.0
pywin32>=306

# Interactive development
ipython>=9.0.0
ipykernel>=6.0.0
